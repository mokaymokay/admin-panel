// add element we just created to the DOM
document.getElementById("all_students").insertAdjacentHTML("beforeend", "<%= j render partial: 'cohorts/cohort_student', locals: { student: @grade.student, cohort: @grade.cohort } %>");
// display flash messages
$("#flash_messages").html("<%= j (render partial: 'shared/flash' , locals: { flash: flash }).html_safe %>");
// reset form
// document.getElementById("new_grade").reset();
// scroll to top
$(document).ready(function () { $('html, body').animate({scrollTop:0}, 100); });
// refresh form
$('#new_grade').on('ajax:success', function(event, data, status, xhr) {
  console.log("success");
  $('#new_grade_container').load( document.URL + ' #new_grade_container', function() {
    console.log("load performed!");
  });
});
