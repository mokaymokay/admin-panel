<div id="flash_messages">
  <% if !flash.blank? %>
    <% flash.each do |type, message| %>
      <div class="<%= flash_class(type) %>">
        <%= message %>
      </div>
    <% end %>
    <% flash.clear %>
  <% end %>
</div>
