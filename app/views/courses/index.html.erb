<% @page_title = "Courses" %>

<h4><%= @page_title %></h4>

<button class="btn btn-outline-primary btn-sm"><%= link_to("Add New Course", new_course_path, :class => 'action new') %></button>

<table class="table table-hover">
  <thead>
    <tr>
      <th></th>
      <th>Name</th>
      <th>Hours</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <% @courses.each do |course| %>
    <tr>
      <td><%= course.id %></td>
      <td><%= course.name %></td>
      <td><%= course.hours %></td>
      <td><button class="btn btn-outline-primary btn-sm"><%= link_to("Edit", edit_course_path(course), :class => 'action edit') %></button></td>
      <td><button class="btn btn-outline-primary btn-sm"><%= link_to "Delete", course_path(course),  :method => :delete, data: { confirm: 'Are you sure?' } %></button></td>
      <td><button class="btn btn-outline-primary btn-sm"><%= link_to("Show Cohorts", course_cohorts_path(course), :class => 'action index') %></button></td>
      </form>
    </tr>
    <% end %>
  </tbody>
</table>
<br>
