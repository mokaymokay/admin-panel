<% @page_title = "Courses" %>

<h4>Courses</h4>

<button ><%= link_to("Add New Course", '#', :class => 'action new') %></button>

<table class="table table-hover">
  <thead>
    <tr>
      <th></th>
      <th>Name</th>
      <th>Hours</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <% @courses.each do |course| %>
    <tr>
      <td><%= course.id %></td>
      <td><%= course.name %></td>
      <td><%= course.hours %></td>
      <td><button type="button" class="btn btn-outline-primary btn-sm"><%= link_to("Show", course_path(course), :class => 'action show') %></button></td>
      <td><button type="button" class="btn btn-outline-primary btn-sm"><%= link_to("Edit", edit_course_path(course), :class => 'action edit') %></button></td>
      <td><button type="button" class="btn btn-outline-primary btn-sm"><%= link_to("Delete", course_path(course), :class => 'action delete') %></button></td>
      </form>
    </tr>
    <% end %>
  </tbody>
</table>
<br>
