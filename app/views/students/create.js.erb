// add element we just created to the DOM
document.getElementById("all_students").insertAdjacentHTML("beforeend", "<%= j render partial: "student", locals: { student: @student } %>");
// manually toggle modal
$("#addStudentModal").modal('toggle');
// display flash messages
$("#flash_messages").html("<%= j (render partial: 'shared/flash' , locals: { flash: flash }).html_safe %>");
// reset form
document.getElementById("new_student").reset();
// scroll to top
$(document).ready(function () { $('html, body').animate({scrollTop:0}, 100); });
