modal = document.getElementById("add-form");
if (modal !== null) {
  modal.outerHTML = "<%= j render partial: "form", locals: { cohort: @cohort, instructors: @instructors, url: course_cohorts_path } %>"
}
