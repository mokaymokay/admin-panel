// add element we just created to the DOM
document.getElementById("all_cohorts").insertAdjacentHTML("beforeend", "<%= j render partial: "cohort", locals: { cohort: @cohort } %>");
// manually toggle modal
$("#addCohortModal").modal('toggle');
// display flash messages
$("#flash_messages").html("<%= j (render partial: 'shared/flash' , locals: { flash: flash }).html_safe %>");
// reset form
document.getElementById("new_cohort").reset();
// scroll to top
$(document).ready(function () { $('html, body').animate({scrollTop:0}, 100); });
